name: virtual-switch
components:
  - id: main
    capabilities:
      - id: switch
        version: 1
    categories:
      - name: Switch
preferences:
  - preferenceId: certifiedpreferences.forceStateChange
    explicit: true
