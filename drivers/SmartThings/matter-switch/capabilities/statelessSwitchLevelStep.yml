name: Stateless Switch Level Step
status: proposed
commands:
  stepLevel:
    arguments:
    - name: stepSize
      schema:
        type: integer
        minimum: -100
        maximum: 100
      optional: false
    name: stepLevel
id: statelessSwitchLevelStep
version: 1