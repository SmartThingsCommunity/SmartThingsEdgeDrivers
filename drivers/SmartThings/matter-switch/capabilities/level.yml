name: Level
status: proposed
attributes:
  level:
    schema:
      $ref: IntegerPercent
    setter: setLevel
  levelRange:
    schema:
      type: object
      additionalProperties: false
      properties:
        value:
          type: object
          additionalProperties: false
          properties:
            minimum:
              type: integer
              minimum: 0
              maximum: 100
            maximum:
              type: integer
              minimum: 0
              maximum: 100
            step:
              type: integer
              minimum: 1
              maximum: 100
          required:
          - minimum
          - maximum
        unit:
          type: string
          enum:
          - '%'
          default: '%'
commands:
  setLevel:
    arguments:
    - name: level
      schema:
        type: integer
        minimum: 0
        maximum: 100
      optional: false
    - name: rate
      schema:
        $ref: PositiveInteger
      optional: true
    name: setLevel
id: level
version: 1