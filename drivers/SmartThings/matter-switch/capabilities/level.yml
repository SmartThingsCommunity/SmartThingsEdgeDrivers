name: Level
status: proposed
attributes:
  level:
    schema:
      value:
        type: integer
        minimum: 0
        maximum: 100
      unit:
        type: string
        enum: ['%']
        default: '%'
    setter: setLevel
  levelRange:
    schema:
      type: object
      additionalProperties: false
      properties:
        value:
          type: object
          additionalProperties: false
          properties:
            minimum:
              type: integer
              minimum: 0
              maximum: 100
            maximum:
              type: integer
              minimum: 0
              maximum: 100
            step:
              type: integer
              minimum: 1
              maximum: 100
          required:
          - minimum
          - maximum
        unit:
          type: string
          enum:
          - '%'
          default: '%'
commands:
  setLevel:
    arguments:
    - name: level
      schema:
        type: integer
        minimum: 0
        maximum: 100
      optional: false
    - name: rate
      schema:
        title: PositiveInteger
        type: integer
        minimum: 0
      optional: true
    name: setLevel
id: level
version: 1