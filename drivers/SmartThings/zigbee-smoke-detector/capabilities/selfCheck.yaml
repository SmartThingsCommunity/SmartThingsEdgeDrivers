id: stse.selfCheck
version: 1
status: proposed
name: Self Check
ephemeral: false
attributes:
  selfCheckState:
    schema:
      type: object
      properties:
        value:
          type: string
          enum:
            - idle
            - selfChecking
            - selfCheckCompleted
      additionalProperties: false
      required:
        - value
    setter: startSelfCheck
commands:
  startSelfCheck:
    name: startSelfCheck
    arguments:
      - name: state
        optional: false
        schema:
          type: string
          enum:
            - startSelfCheck
