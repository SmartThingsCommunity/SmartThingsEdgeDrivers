name: Fan Mode
status: proposed
attributes:
  fanMode:
    schema:
      type: object
      additionalProperties: false
      properties:
        value:       
          title: FanMode
          type: string
          enum:
            - auto
            - low
            - medium
            - high
            - 'off'
    setter: setFanMode
  supportedFanModes:
    schema:
      type: object
      additionalProperties: false
      properties:
        value:
          type: array
          items:  
            title: FanMode
            type: string
            enum:
              - auto
              - low
              - medium
              - high
              - 'off'
commands:
  setFanMode:
    arguments:
    - name: fanMode
      schema:  
        title: FanMode
        type: string
        enum:
          - auto
          - low
          - medium
          - high
          - 'off'
      optional: false
    name: setFanMode
id: fanMode
version: 1