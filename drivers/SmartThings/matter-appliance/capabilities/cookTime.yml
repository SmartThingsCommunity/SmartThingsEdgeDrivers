name: Cook Time
status: proposed
attributes:
  cookTime:
    schema:
      type: object
      additionalProperties: false
      properties:
        value:
          type: integer
          minimum: 1
          maximum: 86400
        unit:
          type: string
          enum:
          - s
          default: s
      required:
      - value
    setter: setCookTime
  cookTimeRange:
    schema:
      type: object
      additionalProperties: false
      properties:
        value:
          type: object
          additionalProperties: false
          properties:
            minimum:
              type: integer
              minimum: 1
              maximum: 86400
            maximum:
              type: integer
              minimum: 1
              maximum: 86400
          required:
          - minimum
          - maximum
        unit:
          type: string
          enum:
          - s
          default: s
commands:
  setCookTime:
    arguments:
    - name: time
      schema:
        type: integer
        minimum: 1
        maximum: 86400
      optional: false
    name: setCookTime
id: cookTime
version: 1