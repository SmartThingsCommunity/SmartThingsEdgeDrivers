name: thermostat
components:
  - id: main
    capabilities:
      - id: temperatureMeasurement
        version: 1
      - id: thermostatMode
        version: 1
        config:
          values:
            - key: "thermostatMode.value"
              enabledValues:
                - off
                - heat
      - id: thermostatHeatingSetpoint
        version: 1
    categories:
      - name: Thermostat